services:
  enterprise-rag-assistant:
    build: .
    container_name: enterprise-rag-assistant
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
      - ./vectorstore:/app/vectorstore